import 'package:flutter/material.dart';
import 'worker_bottom_nav.dart';

class WorkerHome extends StatelessWidget {
  const WorkerHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const WorkerBottomNav();
  }
}
